## 2024-05-06 - LLMAdjudicator Graceful Degradation **Edge Case:** LLM returning valid JSON but with malformed data types (e.g., lists/booleans instead of strings) **Learning:** LLMAdjudicator gracefully degrades these to None, defaulting verdicts to TIE_SCORE (0.5) and omitting malformed constraints **Action:** Always test LLM parsing logic against incorrect primitive types, not just invalid JSON strings
