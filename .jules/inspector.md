## 2024-04-23 - Database Integrity Error on UUID Collision **Edge Case:** Mocking UUID generation to force a duplicate job_id on job creation. **Learning:** SQLite raises a `sqlite3.IntegrityError` when attempting to insert a duplicate primary key, correctly preventing corrupted state. **Action:** We can verify error handling capabilities or database constraints for UUID collisions.
