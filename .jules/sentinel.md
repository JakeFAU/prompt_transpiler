## 2026-05-01 - Fix SQL injection in job store UPDATE query **Vulnerability:** SQL injection vulnerability via string formatting in column names during UPDATE statements. **Learning:** Since parameterized queries cannot be used for column names, they must be strictly validated as identifiers using `.isidentifier()` to prevent injection attacks. **Prevention:** Always validate dynamically constructed column names using `.isidentifier()` before using them in SQL statements.
