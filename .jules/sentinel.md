## 2026-05-14 - Fix SQL injection via dynamic columns **Vulnerability:** SQL injection via dynamic column names in update_job **Learning:** Bandit reports f-string usage for SQL queries even when dynamic components are validated with .isidentifier(). **Prevention:** Add .isidentifier() check to dynamic column names and add '# nosec B608' to suppress false positives on properly sanitized query construction.
