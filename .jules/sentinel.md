## 2025-04-30 - [Fix SQL Injection Vector in SQLite and DuckDB job stores] **Vulnerability:** [SQL injection via dynamic dictionary keys forming columns in UPDATE] **Learning:** [Data structures fed into dynamic SQL must have keys strictly validated (e.g. `isidentifier()`) when string formatting is used, even if parameterizing the values] **Prevention:** [Always validate variable keys to SQL generation routines or refactor architecture to static ORM structures]
