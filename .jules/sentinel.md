## 2025-05-09 - Validate Identifiers in Dynamic SQL **Vulnerability:** SQL injection via dynamic query construction in _to_update_clause **Learning:** While values are parameterized, dynamically added column names (via kwargs) could be manipulated if not validated. **Prevention:** Always use <string>.isidentifier() to validate that dynamically provided column names are safe from SQL injection.
