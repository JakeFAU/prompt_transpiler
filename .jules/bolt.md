## 2025-02-28 - [Performance Bottleneck: LLM Client Instantiation] **Learning:** Repeated initialization of AsyncOpenAI, AsyncAnthropic, and Gemini clients is extremely slow (4.2 seconds for 100 iterations of each) because creating an async client is an expensive operation carrying heavy initialization overhead. **Action:** Apply @functools.cache to get_llm_provider to ensure the instances are cached.
