## 2026-05-14 - Cached LLM provider factory **Learning:** Async LLM clients (like AsyncOpenAI) initialize heavy HTTP connection pools. Without caching, retrieving the client from a factory recreates these pools, causing significant I/O latency. **Action:** Applied @functools.cache to the factory and explicitly cleared it during tests using an autouse fixture to prevent cross-test pollution.
